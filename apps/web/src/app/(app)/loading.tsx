/**
 * Scoped route-transition fallback for the (app) route group. Keeps the global
 * chrome mounted while the authenticated hub streams in. See {@link RouteLoading}.
 */
export { RouteLoading as default } from '@/components/system/route-loading';
