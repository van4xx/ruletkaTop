/**
 * Scoped route-transition fallback for the (economy) route group (top / gifts /
 * premium / wallet / coins). See {@link RouteLoading}.
 */
export { RouteLoading as default } from '@/components/system/route-loading';
