/**
 * Scoped route-transition fallback for the profile routes (/profile/me,
 * /profile/[id]). See {@link RouteLoading}.
 */
export { RouteLoading as default } from '@/components/system/route-loading';
